# Структура проекта OBRAZZ Landing Page

Данный документ описывает полную структуру проекта и назначение каждой директории и файла.

## Обзор архитектуры

```
LandingPageObrazz/
├── 📁 client/              # Frontend (React + Vite)
├── 📁 server/              # Backend (Express.js)
├── 📁 shared/              # Общий код (типы, схемы)
├── 📁 script/              # Build скрипты
├── 📁 Docs/                # Документация
├── 📁 attached_assets/     # Прикреплённые ресурсы
├── 📄 package.json         # Зависимости и скрипты
├── 📄 tsconfig.json        # Настройки TypeScript
├── 📄 vite.config.ts       # Конфигурация Vite
├── 📄 tailwind.config.ts   # Конфигурация Tailwind CSS
├── 📄 drizzle.config.ts    # Конфигурация Drizzle ORM
└── 📄 README.md            # Главный README
```

---

## 📁 client/ — Frontend приложение

Frontend построен на React 18 с TypeScript и использует Vite как сборщик.

### Структура

```
client/
├── index.html              # Точка входа HTML
├── public/                 # Статические файлы (favicon, изображения)
└── src/
    ├── App.tsx             # Главный компонент приложения
    ├── main.tsx            # Точка входа React
    ├── index.css           # Глобальные стили + CSS переменные
    ├── components/         # React компоненты
    │   ├── landing/        # Компоненты лендинга
    │   └── ui/             # Shadcn/ui компоненты
    ├── pages/              # Компоненты страниц
    ├── hooks/              # Custom React хуки
    └── lib/                # Утилитарные функции
```

### client/src/components/landing/

Компоненты секций лендинга:

| Файл | Описание |
|------|----------|
| `HeroSection.tsx` | Главный экран с логотипом, слоганом и iPhone mockup |
| `ProblemSolutionSection.tsx` | Блок "Проблема → Решение" |
| `AppDemoSection.tsx` | Демонстрация приложения с 3 mockups |
| `BenefitsSection.tsx` | 4 карточки преимуществ |
| `HowItWorksSection.tsx` | 3 шага "Как это работает" |
| `SocialFeedSection.tsx` | Демо социальной ленты |
| `PricingSection.tsx` | Тарифы (Free vs Premium) |
| `CTASection.tsx` | Финальный призыв к действию |
| `Footer.tsx` | Подвал с ссылками |
| `IPhoneMockup.tsx` | Переиспользуемый компонент iPhone mockup |

### client/src/components/ui/

Более 50 UI компонентов из [Shadcn/ui](https://ui.shadcn.com/):

- `button.tsx` — Кнопки с вариантами
- `card.tsx` — Карточки
- `dialog.tsx` — Модальные окна
- `toast.tsx` — Уведомления
- И многие другие...

### client/src/pages/

| Файл | Маршрут | Описание |
|------|---------|----------|
| `home.tsx` | `/` | Главная страница (лендинг) |
| `not-found.tsx` | `*` | Страница 404 |

### client/src/hooks/

| Файл | Описание |
|------|----------|
| `use-mobile.tsx` | Определение мобильного устройства |
| `use-toast.ts` | Хук для toast-уведомлений |

### client/src/lib/

| Файл | Описание |
|------|----------|
| `queryClient.ts` | Настройка TanStack Query |
| `utils.ts` | Утилита `cn()` для объединения классов |

---

## 📁 server/ — Backend приложение

Минимальный Express.js сервер для раздачи статики и API.

```
server/
├── index.ts        # Точка входа сервера
├── routes.ts       # API маршруты
├── static.ts       # Раздача статических файлов
├── storage.ts      # In-memory хранилище
└── vite.ts         # Интеграция с Vite dev server
```

### Файлы

| Файл | Описание |
|------|----------|
| `index.ts` | Создание Express приложения, middleware, запуск сервера |
| `routes.ts` | Регистрация API маршрутов (prefix `/api`) |
| `static.ts` | Раздача собранных файлов в production |
| `storage.ts` | Интерфейс `IStorage` и `MemStorage` реализация |
| `vite.ts` | Подключение Vite HMR в development режиме |

---

## 📁 shared/ — Общий код

Код, который используется как на клиенте, так и на сервере.

```
shared/
└── schema.ts       # Drizzle ORM схема + Zod валидация
```

### schema.ts

Содержит:
- Определение таблицы `users` (Drizzle ORM)
- Схема валидации `insertUserSchema` (Zod)
- TypeScript типы `User`, `InsertUser`

---

## 📁 script/ — Build скрипты

```
script/
└── build.ts        # Скрипт production сборки
```

### build.ts

Выполняет:
1. Очистку директории `dist/`
2. Сборку клиента (Vite)
3. Сборку сервера (esbuild)

Оптимизирует бандлинг, включая определённые зависимости для уменьшения холодного старта.

---

## 📁 Docs/ — Документация

```
Docs/
├── PROJECT_STRUCTURE.md    # Этот файл
├── ARCHITECTURE.md         # Архитектура приложения
├── COMPONENTS.md           # Документация компонентов
├── API.md                  # API документация
├── DEVELOPMENT.md          # Гайд по разработке
└── DEPLOYMENT.md           # Инструкции по деплою
```

---

## Конфигурационные файлы

### package.json

Основные секции:
- `scripts` — npm команды
- `dependencies` — production зависимости
- `devDependencies` — dev зависимости

### tsconfig.json

TypeScript конфигурация:
- `strict: true` — строгий режим
- Path aliases: `@/` → `client/src/`

### vite.config.ts

Vite конфигурация:
- React плагин
- Path aliases
- Build output в `dist/public`

### tailwind.config.ts

Tailwind конфигурация:
- iOS-style border-radius
- Custom цвета из CSS переменных
- Кастомные spacing values

### drizzle.config.ts

Drizzle ORM конфигурация:
- PostgreSQL dialect
- Схема из `shared/schema.ts`
- Миграции в `./migrations`

### components.json

Shadcn/ui конфигурация:
- Стиль компонентов
- Alias пути
- Tailwind конфигурация

---

## Алиасы путей

| Алиас | Путь |
|-------|------|
| `@/` | `client/src/` |
| `@shared/` | `shared/` |
| `@assets/` | `attached_assets/` |

### Использование

```typescript
// Вместо
import { Button } from '../../../components/ui/button';

// Используйте
import { Button } from '@/components/ui/button';
```

---

## Переменные окружения

| Переменная | Описание | Обязательна |
|------------|----------|-------------|
| `PORT` | Порт сервера (default: 5000) | Нет |
| `NODE_ENV` | Режим (`development` / `production`) | Нет |
| `DATABASE_URL` | URL PostgreSQL | Да (для БД) |

---

## Дерево зависимостей

```
App.tsx
└── pages/home.tsx
    ├── HeroSection.tsx
    │   └── IPhoneMockup.tsx
    ├── ProblemSolutionSection.tsx
    ├── AppDemoSection.tsx
    │   └── IPhoneMockup.tsx
    ├── BenefitsSection.tsx
    ├── HowItWorksSection.tsx
    │   └── IPhoneMockup.tsx
    ├── SocialFeedSection.tsx
    │   └── IPhoneMockup.tsx
    ├── PricingSection.tsx
    ├── CTASection.tsx
    │   └── IPhoneMockup.tsx
    └── Footer.tsx
```
